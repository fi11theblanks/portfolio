<% content_for :head do %>
  <%= stylesheet_link_tag 'pages/landing', media: 'all' %>
<% end %>

<section class="hero">
  <img src="<%= @hero_image %>" alt="landing image" />
  <div class="hero-content">
    <h1 class="page-title">Landing</h1>
    <p class="page-text">This is the landing page. Keep it simple, calm, and easy to read.</p>
    <div class="hero-links">
      <a href="<%= about_path %>">About</a>
      <a href="<%= work_path %>">Work</a>
      <a href="<%= contact_path %>">Contact</a>
    </div>
  </div>
</section>
